<div class="prduct">
  <div class="page-width">
    <h2>{{ product }}</h2>

    <p>{{ product.description }}</p>
    <br>

    {% for image in product.images %}
      <img src="{{ image | image_url: "large" }}" <br>
    {% endfor %}
    {{ product.price }}
    <br>
  </div>
</div>

<div>
  {{ page.content }}
</div>
<div>Handel{{ page.handle }}</div>
